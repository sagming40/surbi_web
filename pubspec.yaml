name: surbi_web
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1      # ← Task 1-3 추가
  riverpod_annotation: ^2.3.5   # ← Task 1-3 추가
  go_router: ^14.2.0            # ← Task 1-4 추가
  flutter_web_plugins:           # ← Task 1-4 추가
    sdk: flutter                # ← Task 1-4 추가 ^대신 sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  riverpod_generator: ^2.4.0    # ← Task 1-3 추가
  build_runner: ^2.4.8          # ← Task 1-3 추가

flutter:
  uses-material-design: true